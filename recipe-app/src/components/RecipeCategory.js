import React from 'react';

const RecipeCategory = ({ category }) => {
  return (
    <div className="container mt-5">
      <h2>{category} Recipes</h2>
      {/* Add logic to display recipes for the given category */}
    </div>
  );
};

export default RecipeCategory;
